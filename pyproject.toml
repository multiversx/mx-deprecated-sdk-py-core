[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mx-sdk-erdpy-core"
version = "0.4.0"
authors = [
  { name="MultiversX" },
]
license = "MIT"
description = "Package moved (renamed)"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
]
dependencies = [
]

[project.urls]
"Homepage" = "https://github.com/multiversx/mx-sdk-py-core"
