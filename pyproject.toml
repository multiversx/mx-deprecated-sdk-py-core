[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "multiversx-sdk"
version = "0.1.0"
authors = [
  { name="MultiversX" },
]
license = "MIT"
description = "Reserved package name"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
]
dependencies = [
]

[project.urls]
"Homepage" = "https://github.com/multiversx"
