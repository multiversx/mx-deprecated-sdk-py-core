[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mx-sdk-build-contract-rs"
version = "5.0.0"
authors = [
  { name="MultiversX" },
]
license = "MIT"
description = "Package moved (renamed)"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
]
dependencies = [
]

[project.urls]
"Homepage" = "https://github.com/multiversx/mx-sdk-rust-contract-builder"
